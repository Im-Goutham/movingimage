export enum MODE {
  ADD,
  EDIT,
}

export enum SORT_DIR {
  ASC,
  DSC,
}

export enum SORT_TYPE {
  NAME = 'name',
  AUTHOR = 'author',
  CATEGORIES = 'catergories',
  HIGHEST_QUALITY_FORMAT = 'highest_quality_format',
  RELEASE_DATE = 'release_date',
}
